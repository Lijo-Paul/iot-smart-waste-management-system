#include <ESP32Servo.h>

#define SERVO_PIN 21

Servo testServo;

void setup() {

  testServo.setPeriodHertz(50);
  testServo.attach(SERVO_PIN, 500, 2400);
}

void loop() {

  testServo.write(90);
  delay(2000);

  testServo.write(0);
  delay(2000);
}
